= simple_form_for(@gear) do |f|
  = f.error_notification
  = f.input :name
  = f.input :gear_type_id, collection: GearType.all
  = f.input :description
  = f.input :quantity
  = f.input :climber_id, collection: Climber.all if current_user.role == 'admin'
  = f.button :submit, 'Save'

